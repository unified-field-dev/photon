# Infra

Local `infra/broker` helpers remain in-repo when present.

AWS smoke and fleet campaigns (E2E gates, remote check/clippy, broker fleets) run on AWS EC2. Operator runbooks stay outside this public tree.
