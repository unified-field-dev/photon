# Infra

Local `infra/broker` helpers remain in-repo when present.

**AWS smoke and fleet campaigns** live in the private sibling checkout:

`~/unified-field/uf-live-cloud-lab/photon/infra/aws`

Set `UF_LAB_ROOT` and see that repo’s README.
